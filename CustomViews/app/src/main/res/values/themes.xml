<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CustomViews" parent="Theme.AppCompat.DayNight">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimary </item>

        <item name="buttonStyle">@style/meuBotao</item>
        <item name="radioButtonStyle">@style/meuRadio</item>
    </style>

    <style name="Theme.CustomViews" parent="Base.Theme.CustomViews" />
    <style name="estiloExemĺo">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/verde</item>
        <item name="android:backgroundTint">@color/vermelho </item>
        <item name="android:textStyle">italic </item>
    </style>
    <style name="meuBotao" parent="android:Widget.Button">
        <item name="android:background">
            @drawable/selectot_botao
        </item>
        <item name="android:textColor">
            @drawable/selector_texto
        </item>
    </style>
    <style name="meuRadio" parent="android:Widget.CompoundButton.RadioButton">
        <item name="android:button">@drawable/selector_radio</item>
    </style>
</resources>